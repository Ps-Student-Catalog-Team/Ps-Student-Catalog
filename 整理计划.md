# 服务器结构整理计划

## 问题分析
1. 文件和目录组织不清晰，服务器功能与网页功能混合
2. `startnode.bat`中的路径与实际工作目录不匹配
3. 存在一些可能不再需要的文件和目录
4. 缺少明确的项目结构文档

## 整理方案

### 1. 重新组织目录结构
- 创建`server`目录，存放服务器相关文件
  - `server.js`
  - `package.json`
  - `package-lock.json`
  - `lastOnlineTimes.json`
  - `startnode.bat`
- 创建`public`目录，存放网页相关文件
  - HTML文件
  - CSS文件
  - JS文件
  - 图片资源
- 创建`utils`目录，存放工具脚本
- 创建`logs`目录，存放日志文件

### 2. 修复启动脚本
- 修正`startnode.bat`中的路径问题

### 3. 清理不必要的文件
- 删除不再需要的测试文件和过时资源

### 4. 创建项目文档
- 更新`README.md`，说明项目结构和使用方法

## 实施步骤
1. 先创建新的目录结构
2. 移动文件到对应目录
3. 修复启动脚本
4. 清理不必要的文件
5. 更新项目文档